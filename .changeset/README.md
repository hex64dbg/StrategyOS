# Changesets

This folder is managed by `@changesets/cli`.

- Create a changeset for your work with `pnpm changeset`.
- Apply queued versions with `pnpm version`.
- Publish (or skip for private apps) with `pnpm release`.

